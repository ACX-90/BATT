# 05-b 车上增量方案头脑风暴
Powered by SpaceXAI Grok 4.6

日期：2026-08-21

> 约束：[`Doc/05-a`](05-a-车上增量约束评估.md)（不 OTA、不外发、单 MCU 边角料）  
> 基线：第 0 期 A–H；第 1 期默认已锁 **全局 \(k\) → \((s,T)\) 上的 \(k\) 网格**  
> 目的：把还可能的路子摊开，标「可上车 / 可后排 / 实验室 / 丢掉」，避免第 1 期只盯着残差头  
> 本文只做枚举和初评，不改代码、不定最终赢家

信封没变：可变参数要少、按芯隔离、RAM 不攒波形、反传离开 0.1 s 环、损失用开环 \(e^{\mathrm{ol}}\)、没激励不动 \(R\)。下面每条都按这几条筛。

---

## 已经放进第 1 期的（不重复发明）

| 档 | 每芯可变 | 擅长 | 短板 |
|----|----------|------|------|
| 冻结 | 0 | 同分布 | 有墙时什么都不做 |
| \(\delta R_0\) | 1 | 这一趟表偏 | 不是曲面 |
| 全局 \(k_0,k_1\) | 2 | 整张乘性涨阻（A/G） | 低温多涨会抬中温 |
| \((s,T)\) 上的 \(k\) 网格 | 几十 × 2 | 局部涨阻、车到过的温区 | \(I\) 维残差、鼓包形状 |
| 冻住的共享表 | 0（全包一份） | BOL 曲面 | 车上不训 |

3×8×2 头只对照。外置 Flash 只给 \(k\) 做事件环，不是方案本身。

---

## 一类：比网格更瘦的物理乘子

**1. 阿伦尼乌斯一个数。**  
\(k(T)=\exp\bigl[B(1/T-1/T_{\mathrm{ref}})\bigr]\)，每芯 1～2 个数（\(R_0/R_1\) 可共用 \(B\) 或分开）。低温自然比中温涨得多，比 20 个格子更贴老化机理，也更省。  
风险：真实偏差不是纯 \(1/T\)（SOC 两端、充放）时，\(B\) 会拧。  
**可后排**：1b 网格若发现「几乎是冷端一个斜率」，就收成 \(B\)。

**2. 低秩模态。**  
出厂用若干张「差表」当模态：第 1 张全体 1（全局缩放），第 2 张按 \(1/T\)，第 3 张按 SOC 两端。每芯 2～3 个系数。网格是这些模态的逐点版；若老化真是低秩，系数比 20 个 \(k\) 更稳。  
前提：实验室要有几只老化样件才能做 PCA / 指定模态。没有样件就先别上。  
**可后排**，不要和第 1 期网格绑在一起。

**3. 只动 \(k_0\)，\(k_1\) 钉 1。**  
欧姆边沿最可观。极化通道经常拆错（第 0 期微调）。更瘦、更不容易把回弹写成假 \(R_1\)。  
**可上车当 1a 的对照档**：无回弹时本来就不该动 \(k_1\)。

**4. 充放各一套 \(k\)。**  
\(k_{\mathrm{dch}},k_{\mathrm{chg}}\)，共享表已经有方向因子时可能多余。  
**可后排**，先看共享表的 \(f_{\mathrm{dir}}\) 还剩多少残差。

**5. 包级一个 \(k\) × 每芯一个 \(\delta R_0\)。**  
180 串共用电流，真老化往往先是「整包慢慢涨」+「个别芯跑出来」。可变参数从 180×40 变成 2+180。  
**可上车，和第 2 期包级一起做。** 全包同向先走包级 \(k\)，不要 180 份网格一起爬。

---

## 二类：表和插值的变体

**6. 一维 \(k(T)\) 或 \(k(s)\)。**  
4～6 个节点。比 5×4 更瘦。若第 0 期式的错配主要是温度（Arrhenius）或主要是 SOC（U 形），一维就够。  
**可上车作为 1b 的瘦身对照**（先 1D 温度，再 2D）。

**7. 加性 \(\Delta R(s,T)\) 表。**  
和 \(k\) 网格同结构，改成加。老化更像乘；加性要夹正、低温相对误差怪。  
**丢掉当默认**，除非要补「某点偏 50 µΩ」这种绝对量，那用 \(\delta R_0\) 更干净。

**8. 仿射 \(R'=kR+b\)。**  
每节点 4 个数，体积翻倍，和 \(\delta R_0\) + \(k\) 抢活。  
**丢掉。**

**9. 不规则点 / RBF / 距离加权。**  
波动大的地方多放点。车上增删节点难认证；IDW 比双线性贵、边界糊。  
**标定点位出厂加密，几何冻住。** 不要运行时长点。

**10. 把共享 MLP 烤成 \((I,s,T)\) 查找表。**  
这是压缩，不是增量。省 ROM/CPU，\(k\) 网格照乘。蒸馏小网（3×16×16×2）同类。  
**可后排**，等 64×64 前向占核比量过再做。

**11. 共享表也按 \((s,T)\) 分片，每片一个小仿射。**  
等于把 64×64 拆成许多局部线性。和烤表同类，增量仍应只动每芯 \(k\)。  
**不必单独立项。**

---

## 三类：不走电压反传，走辨识

**12. 脉冲直接估 \(R_0\)。**  
边沿 \(\Delta U/\Delta I\) 减掉 OCV 和已知 \(U_p\)，得到 \(\hat R_0\)，再 \(k_0=\hat R_0/R_0^{\mathrm{fleet}}\)，写入最近节点。**不用反传 ECM。** MCU 上就是几次减和除。  
短板：要边沿；\(R_1\) 仍要回弹或放弃。和门控天然同一套。  
**很值得当 1a/1b 的无梯度对照。** 电压 BPTT 在车上越瘦越好。

**13. 回弹窗口估 \(\tau\) / \(R_1\)。**  
静置后指数拟合。方案 B 下 \(C_1\) 钉死，\(\tau\) 直接就是 \(R_1\)。窗长要到十几秒，和滑窗协议一致。  
**可与 12 配对**，不要和开环电压 SGD 同时抢同一段回弹。

**14. 分箱 RLS。**  
每个 \((s,T)\) 箱子一个遗忘因子 RLS 估 \(R_0\)。经典在线辨识，和 MLP 增量不是一层：记住的是「这个箱子这一周的电阻」，箱子之间不插值就会台阶。  
**可后排**，当作 \(k\) 网格的「无插值、无共享表」对照。箱子数一多就和网格重复。

**15. 双 EKF / 随机游走 \(R_0\)。**  
就是 \(\delta R_0\) 再激进一点。跟的是这一分钟，不是曲面。  
**默认保持慢 \(\delta R_0\)**，不要把 \(R\) 放进每拍卡尔曼当主增量。

**16. 中位数 / 剪枝而不是 SGD。**  
同一节点上多次 \(\hat k\) 取中位数再写入，抗个别坏边沿。无学习率、无梯度。  
**可叠在 12 上**，几乎零成本。

---

## 四类：更像「小网络」的适配器

**17. 解冻 64×64 最后一层。**  
约 130 个数/芯，180 串约 90 KB，超信封；且无 Replay 会忘。  
**丢掉。**

**18. LoRA / 低秩增量打在宽网上。**  
秩 1 打末层仍要几十个数，且特征空间全耦合，遗忘回来了。  
**丢掉当车上方案。** 实验室可以拿来和 \(k\) 网格比容量。

**19. 每芯一只更瘦的残差 MLP（3×4×2）。**  
大约 26 个数，和 18 个数头同一类病：特征不够、无局部隔离。  
**丢掉**，不如 \(k\) 网格。

**20. 每模组一份 \(k\)，不是每芯。**  
模组内串数共用温度，参数 ÷10。不一致大的包会亏。  
**可后排**当 RAM 再紧时的退路。

**21. 弹性权重巩固 / 突触智能。**  
要 Fisher 信息，宽网上是矩阵，MCU 没地方放。  
**丢掉。**

---

## 五类：存储与提交策略（不是新函数类）

这些可以叠在 \(k\) 网格上，本身不是另一种 \(R(\cdot)\)。

**22. 影子表 + 验收再提交。**  
先写到影子 \(k\)，一段 NIS / 休息 SOC 变好才换正式表，否则丢弃。防一次坏脉冲写飞。  
**建议叠进 1a。**

**23. 节点带方差。**  
去的少、激励差的节点步长小或不动。贝叶斯味道，实现就是每节点一个计数器。  
**建议叠进 1b。**

**24. 只在节点中心附近才更新。**  
插值权重 < 0.5 的顶点不动，减少「路过」污染。  
**可试。**

**25. 外置 Flash 事件环。**  
05-a 已评：给 \(k\) 混旧窗，不解锁宽网 Replay。  
**可选 BOM，不是算法档。**

**26. 双季节表。**  
夏/冬两套 \(k\)，按月切换。等于把温度维从数据里拿出来用日历硬切，容易切错。  
**丢掉**，用 \(k(T)\) 更干净。

---

## 六类：其实不该叫电阻增量

**27. 包级电流零偏 / 容量慢状态。**  
第 0 期 F：开环很大、滤波仍健康。动 \(R\) 是错的。  
**要做，但是滤波层，第 2 期包级。**

**28. 每芯 OCV 微偏。**  
静置常偏有时是表，不是 \(R_0\)。车上改 OCV 危险，05-a 仍归实验室重标。  
**车上丢掉。**

**29. 给 MLP 加 SOH 输入。**  
换模型，Plan 已锁死。现场 \(q\) 比 \(R_0\) 难测。  
**本期丢掉。**

**30. 升 2RC。**  
换 BMS。H 的慢尾巴允许残差。  
**本期丢掉。**

---

## 怎么排（头脑风暴收束）

第 1 期主线不动：滑窗全局 \(k\) → \((s,T)\) 网格。从上面**只加对照，不加新主线**：

| 优先级 | 方案 | 为什么现在就值得想 |
|--------|------|--------------------|
| 1a 对照 | 只动 \(k_0\)；脉冲 \(\Delta U/\Delta I\) 无梯度 | 车上能不能不反传 |
| 1a 工程 | 影子表再提交；节点计数器 | 几乎不占资源，防写飞 |
| 1b 瘦身 | 先 \(k(T)\) 一维，再 2D | 20 个点会不会浪费 |
| 1b 机理 | 阿伦尼乌斯 \(B\) | 网格若呈 \(1/T\) 就收成 1 个数 |
| 2 期 | 包级 \(k\) + 每芯 \(\delta R_0\)；包级 \(b_I\) | 180 串同向是分流器还是老化 |
| 有老化样件再排 | 低秩模态 2～3 个系数 | 可能比网格更稳 |
| 明确丢掉 | 解冻末层、LoRA、更瘦残差 MLP、EWC、车上改 OCV / SOH / 2RC | 和信封或第 0 期结论打架 |

判断句：**函数类优先选「乘在冻表上的、带 \((s,T)\) 局部性的少参数」；算法优先选「有激励才写、能回退」；能不用反传就不用。** 残差小网、解冻宽网、云端 Replay 都不进这个集合。
