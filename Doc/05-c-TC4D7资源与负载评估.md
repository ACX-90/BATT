# 05-c TC4D7 资源与负载评估
Powered by SpaceXAI Grok 4.6

日期：2026-08-21

> 芯片：英飞凌 AURIX™ TC4D7 / TC4Dx（公开规格，见 `Doc/A0-a`）  
> 代码：[`Src/MCU_Eval/`](../Src/MCU_Eval/readme.md)，标量 TriCore，**没用 PPU**  
> 目的：把第 1 期车上算法（冻表 + \(k\) 网格 + EKF）落到「一拍多少周期、占核百分之几、ROM/RAM 多少」  
> 测法：C 代码拷进英飞凌工程跑。权重随机，只测负荷不测精度  
> 实测：[`Src/MCU_Eval/log/eval_log_v1.0.txt`](../Src/MCU_Eval/log/eval_log_v1.0.txt)（标量核，负载按宏 400 MHz × 0.1 s）

---

## 1. 芯片信封（算法能看见的）

| 项 | 公开量级 | 本评估怎么用 |
|----|----------|----------------|
| CPU | 最多 6× TriCore 1.8，400～500 MHz | 负载按 **400 MHz、单核、0.1 s SoX 环** 报；500 MHz 把结果 ×0.8 |
| SRAM | 约 10 MB 整片 | 不是算法私有。对照仍看 `Doc/05-a` 的 16～32 KB 稳态信封 |
| NVM | 约 20 MB | 权重应进 Flash；本 C 里随机数先放 BSS，报表时把权重字节**计到 ROM** |
| PPU | 256 bit SIMD | 本套 C **不上 PPU**。标量核先给出下限 |

一拍预算：\(400\,\mathrm{MHz}\times 0.1\,\mathrm{s}=4\times 10^7\) 周期。  
负载 \(=\)（一拍总周期）/ \(4\times 10^7\)。180 串顺序跑时，总周期 ≈ 单芯 × 180。

---

## 2. 代码怎么拷、怎么读日志

始终加入：

- `Src/MCU_Eval/mcu_eval_common.c`
- `Src/MCU_Eval/mcu_eval_common.h`
- `Src/MCU_Eval/mcu_eval_algos.h`

再按 [`Src/MCU_Eval/readme.md`](../Src/MCU_Eval/readme.md) 加一份 `eval_*.c`。时钟起来后：

```c
McuEval_Init();
McuEval_SoxPack_Bench(); /* 或其它 Bench */
/* ASCLIN 发送 McuEval_LogBuf()，长度 McuEval_LogLen() */
```

日志在 **`g_mcu_log[64*1024]`** 里，不必边算边打串口（串口会污染周期）。Bench 结束后整块发出去。

`McuEval_Init()` 在 TASKING 上会打开 CPU_CCNT（`CCTRL=0xFC00`，`CCNT=0xFC04`）。若和 iLLD 冲突，改 `mcu_eval_common.c` 里的 `McuEval_GetCycles` 即可。

单文件自测：给那一个 `eval_*.c` 加预定义 `MCU_EVAL_STANDALONE`（不要同时开多个）。一次跑完全部：加 `eval_all.c`，不要开 `STANDALONE`。

请回传：

1. 完整 `g_mcu_log` 文本  
2. map 里各模块 `.text / .rodata / .bss`（若方便）  
3. 实际核频（400 还是 500）、是否 lockstep、有没有关中断  

---

## 3. 测哪些算法

| Bench | 文件 | 对应方案 | 环 |
|-------|------|----------|----|
| `ecm1` | `eval_ecm.c` | 一阶 Thevenin 单步 | 10 Hz |
| `ekf_s_up` | `eval_ekf.c` | 二维 EKF | 10 Hz |
| `ekf_s_up_dr0` | 同上 | 开 \(\delta R_0\) 的 2 态（残差通道仍很轻） | 10 Hz |
| `mlp_3x64x64x2` | `eval_mlp64.c` | 冻结共享表 | 10 Hz |
| `mlp_3x16x16x2` | `eval_mlp16.c` | 压缩表对照 | 10 Hz |
| `head_3x8x2` | `eval_head3x8x2.c` | demo 残差头 | 10 Hz |
| `k_global` | `eval_k_global.c` | 每芯 2 个数 | 10 Hz |
| `k_grid_5x4` | `eval_k_grid.c` | 每芯 20 点插值 | 10 Hz |
| `sox_cell_...` | `eval_sox_cell.c` | **单芯完整一拍**（MLP64+\(k\) 网格+EKF） | 10 Hz |
| `sox_pack180_...` | `eval_sox_pack.c` | **180 芯顺序一拍**，RAM 按真包铺 | 10 Hz |
| `incr_kgrid_win100` | `eval_incr_kgrid.c` | 窗 100 累积再 SGD | **降频 / 停车**，不要当 10 Hz |
| `pulse_dU_dI_k0` | `eval_pulse_r0.c` | 边沿估 \(k_0\)，无反传 | 事件 |

`eval_sox_cell.c` / `eval_sox_pack.c` / `eval_incr_kgrid.c` 还要链上它们依赖的 `eval_mlp64.c`、`eval_ekf.c`、`eval_k_grid.c`。

---

## 4. 纸面量级（芯片数回来之前）

乘加按 float32 标量、GELU≈一次 tanh 估。周期按「一次 MAC 约 4～8 拍、GELU 几十拍」的粗框，**不是承诺**。

| 块 | 可变数据 | 权重量（应进 ROM） | 单次大约乘加 | 纸面：单芯周期量级 | 180 串占 400 MHz×0.1 s |
|----|----------|-------------------|--------------|--------------------|------------------------|
| ECM 单步 | 几个 float | 0 | 几次 + 1 次 exp | \(10^2\) | ≪0.1% |
| EKF 2 态 | ~50 B/芯 | 0 | 几十 | \(10^2\sim10^3\) | <1% |
| 3×8×2 | 72 B/芯头 | 200 B 共享 | ~50 + tanh | \(10^2\sim10^3\) | <1% |
| \(k\) 全局 | 8 B/芯 | 0 | 2 | \(10^1\) | ≪0.1% |
| \(k\) 5×4 插值 | 160 B/芯 | 0 | ~20 | \(10^2\) | <1% |
| MLP 16×16 | 0 | ~1.5 KB | ~370 + 32 GELU | \(10^3\sim10^4\) | 约 1% 量级 |
| MLP 64×64 | 0 | **~18 KB** | ~4400 + 128 GELU | \(10^4\sim10^5\) | **可能到数 %～十几 %** |
| 单芯 SoX | 网格+EKF | 18 KB 共享 | 上面几项之和 | 跟 MLP64 | 跟 MLP64 |
| 窗 100 增量 | 梯度 160 B | 0 | 约 100 次插值+SGD | \(10^4\) 量级/窗 | 不进 10 Hz |
| 脉冲 \(k_0\) | 4 B | 0 | 几次除法 | \(10^1\) | 事件 |

纸面把 MLP64×180 估成「数 %～十几 %」，**偏小**。v1.0 实测单芯约 \(9.7\times10^4\) 周期，180 串约 **44%**（§5）。

RAM 纸面（算法，不含 64 KB 日志）：

| | 1 芯 | 180 芯 |
|--|------|--------|
| EKF | ~50 B | ~9 KB |
| \(k\) 5×4 | 160 B | **29 KB**（int16 可腰斩） |
| MLP 激活（复用） | ~0.5 KB | 0.5 KB |
| 合计（SoX） | <1 KB + 共享权重 | **约 40 KB 态 + 18 KB 权重** |

权重应在 ROM。29 KB 的 \(k\) 网格贴着 05-a 的 32 KB 稳态信封；这是「每芯一份表」的主成本，不是 MLP。

---

## 5. 实测 v1.0

日志：`Src/MCU_Eval/log/eval_log_v1.0.txt`。负载按 **400 MHz、0.1 s、单核标量**。min≈avg，计数器稳定。若板子实际 500 MHz，百分数 ×0.8。

| Bench | cyc_min | cyc_avg | load%（1 芯） | load%（×180） | 备注 |
|-------|---------|---------|---------------|---------------|------|
| ecm1 | 166 | 166 | 0.0004 | 0.07 | |
| ekf_s_up | 573 | 575 | 0.0014 | 0.26 | |
| ekf_s_up_dr0 | 572 | 574 | 0.0014 | 0.26 | 和不开 δR0 一样轻 |
| k_global | 32 | 32 | 0.0001 | 0.01 | |
| k_grid_5x4 | 256 | 286 | 0.0007 | 0.13 | 180 芯表 28.8 KB |
| pulse_dU_dI_k0 | 52 | 52 | 0.0001 | — | 事件 |
| head_3x8x2 | 1391 | 1391 | 0.0035 | 0.63 | demo 头不是瓶颈 |
| mlp_3x16x16x2 | 10752 | 10769 | 0.027 | **4.8** | 压缩表 |
| mlp_3x64x64x2 | 96779 | 96825 | 0.24 | **43.6** | **主项** |
| sox_cell | 97589 | 97649 | 0.24 | **43.9** | 几乎就是 MLP64 |
| sox_pack180 | 17.57e6 | 17.58e6 | — | **43.9** | 真 180 循环，和单芯×180 一致 |
| incr_kgrid_win100 | 53156 | 53158 | （若误当 10 Hz：0.13） | — | **停车跑**，约 0.13 ms |

| 对象 | 计为 | 字节 |
|------|------|------|
| MLP64 权重 | ROM | 18208 |
| MLP16 权重 | ROM | 1504 |
| 3×8×2 | ROM | 200 |
| 180×\(k\) 5×4 | RAM | 28800 |
| 180×EKF | RAM | 7200 |
| SoX 态合计（网格+EKF） | RAM | **36000** |
| `g_mcu_log` | 评测专用，扣除 | 65536 |

拆一拍（pack180）：MLP64 约占 99%，EKF+\(k\) 网格+ECM 合起来不到 1%。单芯 SoX 244 µs；180 串顺序 **44 ms / 100 ms 预算**。

---

## 5.1 读完这张表意味着什么

1. **64×64 共享表按 180 串 × 10 Hz 在 400 MHz 单核上是 44%。** 超过 05-a「算法大约一成核」的紧信封。500 MHz 仍约 35%。这不是 \(k\) 网格的错，是冻住的宽网前向。
2. **换成 16×16，同样 180×10 Hz 大约 5%。** 再加 EKF 和网格也只到约 5%。这才像「边角料」。压缩表 / 查找表要提前，不要等网格做完才压。
3. **增量不占 10 Hz。** 窗 100 一次约 5.3 万周期（0.13 ms）。停车做完全没问题。
4. **RAM 大头是 29 KB 的每芯网格**，不是 MLP。贴着 32 KB 信封。int16 或先用全局 \(k\)（1.4 KB/包）更稳。
5. **3×8×2 仍然很便宜**（180 串 0.6%）。它解决不了 44% 的前向；也不必为 demo 头开 PPU。
6. **要不要 PPU：** 继续用 64×64 标量 SoX 的话，44% 已经值得把 180 芯前向丢给 PPU。若改 16×16 / LUT，标量核就够，PPU 仍留给诊断网。

建议顺序：第 1 期协议仍是滑窗 \(k\)；**车上推理默认不要 64×64×180×10 Hz**，先 16×16 或烤表，再决定 PPU。

---

## 6. 读数时别踩的坑

- **不要把串口发送算进 `GetCycles`。** Bench 内部已不含发送。
- **min 比 avg 干净**：中断、cache 冷启动会抬 avg。关中断再报一份 min。
- **主机 `Script/mcu_eval_host.bat` 的周期不是 TC4D7**（PC 主频和 libm 都不同）。只能确认能链接、日志格式对。我这边主机冒烟已通过。
- **GELU / tanh / exp** 在 TriCore 上往往比 MAC 还贵。若 MLP64 偏大，先把 GELU 换成 ReLU 再测一档，看激活函数占多少——不改训练结论，只拆负荷。
- **pack180 的 RAM** 含 180 份网格。不要和「单芯 bench ×180」的 RAM 搞混。
- 增量窗在停车跑。即使窗 100 要一万周期，除以「一次充电一次」也不是负载问题。

---

## 7. 和 05-a 信封怎么对

| 05-a 要求 | v1.0 回答 |
|-----------|-----------|
| 冻结表 ≤ 20 KB ROM | 18 KB 放得下；**180×10 Hz 前向 44%，超一成核** |
| 每芯可变 ≤ 20 KB 量级 | 5×4 网格 29 KB/包，略超；int16 或先全局 \(k\) |
| RAM 稳态 16～32 KB | 网格 29 KB + EKF 7 KB = 36 KB，网格是大头 |
| 反传离开 0.1 s | 窗 100 仅 0.13 ms，停车足够 |
| 3×8×2 很便宜 | 180 串 0.6%，成立 |

第 1 期协议不变（滑窗 \(k\) → 网格）。车上推理：**不要默认 64×64×180×10 Hz**。16×16 实测约 5%，先走这条或烤查找表。仍用宽网才考虑 PPU 做 180 芯前向。
