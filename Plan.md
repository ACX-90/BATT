# 开发计划
Powered by SpaceXAI Grok 4.6

> 对象：100 Ah NMC，BMS 侧锁死方案 B 一阶 Thevenin + EKF\((s,U_p)\) + 开环增量  
> 对照：`Doc/01`–`04`、`Doc/05-a`、`Doc/A0`，数字 `Doc/04-a`，用法 `Doc/04-b`，车上信封 `Doc/05-a`，仓库现状见 `README.md`  
> 目的：把评估结论收成可执行分期。架构判断已经做完；第 0 期 A–H 已齐。第 1 期增量必须按 [`Doc/05-a`](Doc/05-a-车上增量约束评估.md) 的车上信封做：不靠 OTA、数据不外发、单 MCU 剩下来的 CPU / RAM / ROM。英飞凌 demo 300 s 报 1% SOC 不够当过关，小时级与 \(\hat Q\neq Q\) 就是补这扇窗

默认电路仍是一阶 Thevenin，放电电流为正，\(\Delta t=0.1\,\mathrm{s}\)。BMS 默认方案 B：MLP 只出 \(R_0,R_1\)，\(C_1\equiv C_1^\star\)。

---

## 1. 当前位置

工具链已闭环：解析乘性 1RC → 网格 / 单条 / 小时级仿真 → 方案 B 电压反传 → EKF 估 SOC → `increment.py`（`replay` / `scale` / `finetune`）。合成滤波形态符合 `Doc/03-c`：开环可到二十多毫伏、先验亚毫伏、后验更小；安时漂可被电压拉回。

第 0 期 **A–H 已齐**（`Doc/04-a`）：涨阻走缩放、填洞走 Replay、同分布冻结、测量列不改排位、\(\delta R_0\) 扛一趟表偏、长恒流 / 零偏不许拆 \(R\)；\(q=0.90\) 仍走缩放；2RC 真值留下回弹慢尾巴，BMS 保持 1RC。用法收到 `Doc/04-b`。车上约束收到 `Doc/05-a`。第 1 期 1a / 1b / 1c 已进 `Doc/05-d`：车上默认写 \(k\) 网格，3×8×2 丢掉。下一件第 2 期包级。Replay 退出车上路径。

| 层 | 已有 | 没有 / 很弱 |
|----|------|-------------|
| 仿真 | 乘性 1RC、网格、噪声列、小时级工况档 | 2RC；SOH 因子；真 HPPC |
| MLP | A / B / B+、教师预热、整段 BPTT、`--use-meas-inputs` | 默认仍真值列；A / D 对照按 100 轮底板（7.8 / 11.8 mV） |
| KF | \((s,U_p)\)、三种残差、NIS、门控、\(\delta R_0\) 对照 | \(\delta R_0\) 默认仍关 |
| 增量 | Replay / 缩放 / 只微调、冻 scaler、A/B/C 遗忘对照表；车上滑窗 \(k\) / 网格 | 车上无 Replay；18 个数头已对照并丢掉 |
| 边缘 | 评估到 TC4D7 / 10 mV；`Doc/05-a` 紧信封 | 任何 MCU 代码（不做）；PC 须按信封仿真 |

---

## 2. 锁死的判断

这些题 `Doc/03` 与 `Doc/A0` 已经答完，计划期内不再立项。

- 点式 `ParamMLP` + 方案 B 留下；不要用 GRU / LSTM 换骨干
- 不要增量期切方案 A，或让 \(C_1\) 逐步自由
- 不要把 MLP 权重塞进 EKF；不要用 \(e^{\mathrm{post}}\) 或 \(s^+\) 反传
- 不要原样抄「\(\lvert e\rvert>10\,\mathrm{mV}\) 就每 0.1 s 反传」
- 不要为 MCU / PPU 写固件
- 不要把日常增量设计成「云端训完 OTA 回写」或「日志外发再 Replay」
- 不要在车上解冻 64×64、不要 Adam、不要整段 BPTT、不要按轨迹攒波形
- 不要现在给 MLP 输入加 SOH 维（现场 \(q\) 比 \(R_0\) 还难测；那是换模型）
- 不要再写和 03 / A0 同级的结构评估长文（车上约束以 `Doc/05-a` 为准）
- `--resume` 同一网格接着跑 **不是** 增量

学习笔记（在线辨识、RLS、教师参数、雅可比、\(\ln\tau\)）可继续补概念，不挡实验。

---

## 3. 主线原则

1. **BMS 锁一阶。** 估计、滤波、增量、残差头都建立在可查询的 \(R(I,s,T)\) 上。时序记忆在 \(U_p\)，不在网络。
2. **仿真可以比 BMS 更真。** 错配必须可开关、可归因，不要第一次生成就全开。
3. **增量只吃 \(e^{\mathrm{ol}}\)**，SOC 用 \(s^{\mathrm{ah}}\)（或最多 \(s^-\)）。冻 `scaler.json`，禁止预热，禁止静默 `fit_scaler`。
4. **缺维时不硬增量。** 整体涨阻走 \(k_0,k_1\)；填洞走 Replay（**仅实验室 / 下线**）；跨寿命要么另一张表，要么以后换四输入模型。车上没有旧网格，填洞的正确动作是不更新。
5. **车上增量按 `Doc/05-a` 信封。** 默认写 \((s,T)\) 上的 \(k\) 网格（只动被点到的节点）；1a 的两个 \(k\) 是 1×1 特例，节点齐了再收，不要更新前猜全局还是局部。18 个数头已对照并丢掉。滑窗累加梯度，不存波形；反传离开 0.1 s 环。
6. **先要数字，再写新模块。** 没有四档 × 三任务对照表，不宣称 Replay 优于微调，不上残差头当默认。第 1 期对照已是「无旧集」的滑窗 \(k\) / 网格 / 头，不是把第 0 期 Replay 再跑一遍。

---

## 4. 必须先回答的四个问题

| 编号 | 问题 | 怎么答 | 不靠什么 |
|------|------|--------|----------|
| Q1 | 同分布增量会不会假装成功？ | 填洞 / 换对象 / 老化 三组差异上跑全量重训、只微调、Replay、缩放 | 再训同一网格 1000 epoch |
| Q2 | 激励不足时电压变好会不会拆错 \(R\)？ | 长恒流 + 停车当新年份；门控应拒；强行微调只报 \(R_0+R_1\) | 加大网、换 RNN |
| Q3 | 测量列相对真值列偏多少？ | A、D 都训 100 轮；A 冻结 7.8 mV，D 抬噪声后 11.8 mV（+4 mV） | 真值列权重直接当现场权重；拿 500+ 的 4 mV 来减 |
| Q4 | 开环残差长什么样？ | 先画边沿 / 回弹 / 稳态，再决定 2RC 或 B+ | 先换 backbone |

---

## 5. 仿真比 BMS 更真（数据源，不是估计器）

默认生成器不变：`Data/grid/` = BOL + 1RC + 约 10 min SEQUENCE（有边沿、有回弹）。新年份写到新目录，禁止为迁就增量而改掉 `gen_grid` 清目录的默认行为。

### 5.1 生成器加 2RC + SOH，BMS 仍一阶（优先于 PyBaMM）

同一套离散、同一套 CSV、错配可调。BMS 的 `ecm_forward`、`ParamMLP`、EKF 状态 **不读** \(R_2,C_2,q\)。

**2RC 和 SOH 必须能分开开关。** 结构差（缺一条 \(\tau\)）和缺输入维（同一 \((I,s,T)\) 的 \(R\) 已经变了）不是一件事。

| 生成 | 用来验什么 |
|------|------------|
| 1RC，\(q=1\) | 现有网格，舰队预训练 |
| 2RC，\(q=1\) | \(e^{\mathrm{ol}}\) 是否第二指数；1RC 电压上限 |
| 1RC，\(q=0.90/0.80\) | 缺维增量：Replay 忘不忘、缩放够不够 |
| 2RC，\(q<1\) | 最后叠，不作为第一组结论 |

SOH 先用乘性寿命因子，\(q=1\) 时因子为 1：

\[
R = R^{\mathrm{BOL}}(I,s,T)\cdot\bigl(1+a(1-q)\bigr),\quad
C = C^{\mathrm{BOL}}\cdot\bigl(1+a_C(1-q)\bigr),\ a_C<0
\]

量级按 `Doc/01-a`：\(q=0.8\) 时 \(R_0\) 大约 +30%～+100%，先取中间（例如 \(a_0=1\) → \(\times 1.2\)），\(a_1\) 略大。不要一上来做 \(q\) 与 SOC 耦合的鼓包。

**容量 SOH 和电阻 SOH 分成两个旋钮。** 只改 \(R,C\) 验曲面缺维；只改生成器 \(Q=q_Q Q_n\)、EKF 仍 100 Ah 验 \(\hat q\neq q\)（应先估容量，不要开电阻增量）。两个一起开像真老化，第一次对照不用。OCV 随老化变默认关（否则误差进 \(R_0\)，见 03-a §5.10）。

2RC 离散多一条 \(U_{p2}\)。慢支路 \(\tau_2\) 取 **60–120 s**，\(R_2\sim 0.3\sim 0.5\,R_1\)：太快并进 \(R_1\)，太慢现有 120 s 静置看不见。CSV 可多 `r2_ohm,c2_f,u_p2_v,soh` 供对照；教师预热和电压主损失都不许吃。`C_1^\star` 不要为迁就 2RC 去改。参数 schema 升到 `1.1.0`，JSON 写明 `soh`、是否 2RC。

老化轨迹写到 `Data/soh_q90/` 等新目录，**不要**把网格变成 SOC×T×SOH。

### 5.2 小时级序列（给 KF / 门控，不进默认网格）

现有 SEQUENCE 约 10 min、6200 步，对学 \(R(s,T)\) 够。几小时补的是积分误差和长停后的常偏，不是让 MLP 多看几圈电压。

单独工况档，写到 `Data/long/`：

- 0.3C 放电 2 h + 静置 30 min（零偏、容量错）
- 1C 快充 40 min + 停车 2 h（休息点 SOC、静置常偏）
- 中间插几次 10 s 脉冲；长恒流 + 长停车作增量**负例**

约束：训练网格仍短；长轨迹 `--tbptt` 200–400 或走滑窗；静置可 1 s 采样、边沿切回 0.1 s；\(U_{p0}\) 用段首滤波值；必须过 `gate.py`。不要全程 0.1 s 对 MLP 做满小时 BPTT。没有电热耦合时，不要用小时级大电流等温轨迹冒充真车热过程。

### 5.3 PyBaMM（后排）

只当可选电压预言机（建议 SPMe），不替换 `NMC100AhECM`，不让 MLP 出电化学参数，不进 EKF。默认参数集对不上 100 Ah，必须先缩放到本模板的 1C / 电压窗口，否则和「换了一只完全不同的芯」分不开。OCV 仍用仓库 `ocv.py`。排在 2RC 残差已经像真扩散之后。

---

## 6. 分期

### 第 0 期 — 对照表与错配脚手架（先做）

几乎不写新估计算法。没有这张表，第 1 期没有基线。

**前半（1RC 自洽错配，实现量最小）**

1. 合成差异：缩放教师电阻、挖掉某温区、可选电流零偏 / 容量错。新年份新目录。  
   电阻缩放网格：`python Src/Sim/nmc100ah_ecm_gen_grid.py --out-dir Data/soh_k115 --r0-scale 1.15 --r1-scale 1.15`
2. 一次跑四档：全量重训、只微调、Replay、`scale`（外加冻结基线）。  
   `python Src/AI/KF/compare.py --make-new --r0-scale 1.15 --r1-scale 1.15`  
   表：旧 RMSE、新 RMSE、参考点 \(R_0/R_1\)。
3. 训一版测量输入权重；后续增量只挂这版。
4. 打开 `--dr0` 对照：MLP 的 \(R_0\) 故意 ×1.2，确认边沿 \(e^{\mathrm{ol}}\) 变大、休息 SOC 不被长期拉走。
5. 1–2 条小时级负例：门控应拒，或只报 \(R_0+R_1\)。同一条长轨迹打 `--current-bias` 与 `--capacity-scale`。

**后半（生成器升级）**

6. 仿真加可关的 \(q\) 因子 + 可选 2RC 离散。BOL 1RC 网格不变。写出 `Data/soh_q90/`、`Data/rc2/`。
7. 用 \(q<1\)（1RC）重跑四档，得到缺维遗忘的数字；用 2RC、\(q=1\) 画 \(e^{\mathrm{ol}}\) 回弹（Q4）。

交付：数字在 [`Doc/04-a-合成增量对照实验.md`](Doc/04-a-合成增量对照实验.md)（含 G \(q=0.90\)、H 2RC 残差），用法在 [`Doc/04-b-增量学习应用手册.md`](Doc/04-b-增量学习应用手册.md)。电压 RMSE 单独下降不能结题。

### 第 1 期 — 车上增量（先吃 `Doc/05-a`）

第 0 期数字仍是基线。实现场地换成「假装自己是那颗缺资源的 MCU」：读不到旧网格、不能 OTA、不能外发波形、RAM 跟参数走不跟窗长走。仍不写固件。

**约束（写进对照脚本的断言，不是口头）**

- 更新路径禁止读 `Data/grid/` 做 Replay / 合集重训
- 可变参数：先每芯全局 \(k_0,k_1\)（2 个数）；形状任务改 \((s,T)\) 上的 \(k\) 网格（`Doc/05-a`）；舰队表冻住只推理。3×8×2 只作对照，不当车上默认
- 滑窗只累积梯度与门控标量，不存 \(N\times\) 串数的波形
- 优化器 SGD；无 Adam 状态
- 报 ROM / RAM / 每窗乘加，对照 `Doc/05-a` 信封
- 任务 B（填洞）允许失败，标成车上不做
- 外置 Flash **不作为 1a 前置**。有盘只许门控事件环给 \(k\) / 头混旧窗，不许存全程 10 Hz、不许回放宽网（`Doc/05-a`）

**1a. 滑窗 + 只动 \(k_0,k_1\)（先做）**

窗长先试 \(N=50\sim 200\)（5–20 s），窗内 \(R\) 按中心 \((I,s,T)\) 查一次，\(N\) 拍 \(e^{\mathrm{ol}}\) 合成 \(\partial L/\partial \log k\) 再一步。无边沿 / 无回弹不更新。接到 `increment.py` 的车上模式（或平行入口），**默认关掉 Replay**。用第 0 期 A / D / G 无旧集复跑：缩放仍须把新年份贴回底板。代替「电压一直偏就每拍改一次」。

**1b. 每芯 \((s,T)\) 上的 \(k\) 网格（车上默认写点；1a 是 1×1 特例）**

全局 \(k\) 是 1×1 网格。加密到大约 5×4（20 个节点，两端 SOC、低温略密），位置出厂写死，车上只改数值。当前 \((s,T)\) 双线性插值；梯度只回被点到的 2～4 个顶点。\(k\) 乘性、为正、邻点加光滑。同一套滑窗 + 门控。单窗分不出整张涨还是局部涨，更新前不要切 1a。

对照：任务 A 各节点应齐刷到约 1.15；人为只把 −10 °C 电阻抬高时，只冷端节点动、中温几乎不动（这就是车上版「填洞」，不靠 Replay 宽网）。冻结共享表用三维查找表（`Doc/05-c` §9），不要和网格第一次对照绑在一起。

**1c. 3×8×2 残差头，只对照（已跑，丢掉）**

对齐 `Doc/03-d` 式 (6)：\(R=f_{\mathrm{fleet}}+\Delta R_{\max}\tanh(W\varphi+b)\)，冻 \(\varphi\)，只动 18 个数。A 一遍 10.58 mV 不赢 1a 的 6.84；只抬 −10 °C 时中温 2.54→5.63 mV，不赢 1b。车上停在 \(k\) 网格。不要先加宽。

外置 Flash 仍不作为前置。若对照要模拟，只加门控短窗环形缓冲给 \(k\) 节点混旧窗，容量按 16 MB NOR、约 50 个 20 s 窗记账。

### 第 2 期 — 包级对照（PC，不写 MCU）

- 复制 \(N_s=180\) 条轨迹，每条不同 \(k\) 或低温偏置；共用一条带零偏的电流
- 每芯一个 EKF + 一份 \(k\) 网格（先全局 2 个数，再 \((s,T)\) 网格）
- 四档：demo 原样（10 mV / 0.1 s）、`Doc/05-a` 滑窗 \(k\) 网格、3×8×2（1c 已丢掉，只当失败对照）、实验室宽网 Replay（只对照，不上车）
- 硬标准：全包同向 \(\Delta R\) 必须先排除 \(b_I\)
- 评估 \(\delta R_0\) 是否默认打开：吸收「这一趟偏 50 µΩ」；连续多天同号再在车上动 \(k\)，不外发
- 可选：PyBaMM SPMe 适配器，作为第四种错配（模型类错了）

### 第 3 期 — 真数据或残差有结构之后

- 真实 100 Ah NMC：按 `Doc/01-b` §8 重标。只改 JSON，不改乘性结构
- \(e^{\mathrm{ol}}\) 回弹双指数：BMS 仍可保持 1RC；若必须升阶，加 2RC 且 \(C_2\) 钉死或全局慢变（这是换 BMS，单独立项）
- 仍有数 mV 慢尾巴：接法 Δ，残差 GRU 维数 \(\le 4\)、幅度封顶，增量时冻住
- 确认要一张网跨寿命：立项 SOH 输入，当新模型重训
- OCV 表过期先重标 OCV，再动 MLP

---

## 7. 建议顺序

| 顺序 | 做什么 | 完成标准 |
|------|--------|----------|
| 1 | 第 0 期前半：四档 × 三任务 + 测量列权重 + \(\delta R_0\) + 长轨迹负例 | **已完成**（`Doc/04-a` A–F）；无激励不拆 \(R\) |
| 2 | 第 0 期后半：仿真 \(q\) 与 2RC 可关 | **已完成**（`Data/soh_q90/`、`Data/rc2/`；BOL 网格仍 1RC） |
| 3 | 第 1 期约束落地：无 Replay 入口 + 内存记账 | **已有** `window.py`：更新不读旧网格，SGD，窗级门控 |
| 4 | 滑窗只动 \(k_0,k_1\) | **A / D / G 已跑**（`Doc/05-d`：实验室对照；车上是节点已齐后的 1×1，不是冷启动默认） |
| 5 | \((s,T)\) 上的 \(k\) 网格 | **已跑**（`Doc/05-d` §4：车上默认写点；A 四遍 \(k_0\) 齐刷约 1.13；只抬 −10 °C 时中温 \(k\approx 1\)） |
| 6 | 3×8×2 头只对照 | **已跑**（`Doc/05-d` §5：A 一遍 10.58 不赢 1a 6.84；冷端中温 2.54→5.63，丢掉） |
| 7 | 180 串共用电流 | 休息 SOC 钉住；\(k\) 不全包同向涨 |
| 有电芯后再排 | HPPC 重标；视残差决定 BMS 是否 2RC | 参考点与实验室脉冲同量级 |

第 0 期 A–H 已齐。1a / 1b / 1c 已进 `Doc/05-d`。车上默认写网格；1c 丢掉。下一件：第 2 期 180 串。

---

## 8. 验收（沿用文档，不另发明指标）

| 检查 | 通过 |
|------|------|
| 旧网格 / 旧探点开环 RMSE | 不明显差于增量前 `best.pt`（口径：恶化 < 20%） |
| 新轨迹开环 RMSE | 低于冻结权重直接推理 |
| 参考点 \(R_0,R_1\) | 填洞接近旧值；老化单调、幅度可解释（15% 缩放不该学成 80%） |
| 无脉冲新年份 | 只报 \(R_0+R_1\)，不单独信拆开的通道 |
| 休息点 SOC | 增量后不恶化；电压贴上但 SOC 歪了，先查 03-c §5.1 / §5.3 |
| NIS | 回到 \(O(1)\)，不是靠把 \(R_v\) 调爆 |
| 全包同向涨阻 | 先排除分流器零偏 / 容量 |
| \(q<1\) 只微调 | 旧 BOL 网格应明显变差（记成失败对照，证明缺维） |
| 2RC、\(q=1\)，BMS 1RC | 回弹后段允许系统性残差；休息 SOC 不被慢支路长期拉走 |
| 头未更新 | \(R\equiv R^\star\) 或 \(f_{\mathrm{fleet}}\) |
| 车上增量（第 1 期） | 无旧集、无 OTA、无波形缓冲；ROM / RAM / 乘加落在 `Doc/05-a` 信封里 |

---

## 9. 明确不做（一期清单）

- 默认 `gen_grid` / `train.py` 改用 2RC、SOH 维、几小时 SEQUENCE 或 PyBaMM
- 逐步反传整张 MLP；增量方案 A；在 `test.py` 里更新权重
- 解冻 3×8 前层再 0.1 s 反传；用 \(64\times 64\) 去「复现英飞凌」再和 3×8×2 比精度
- 接法 V（RNN 出电压）；双向 RNN；把 \(h\) 扩进 EKF
- 用仿真 `soh` 列监督一个 SOH 头
- 日常增量依赖 OTA 下发或日志外发；MCU 上 Replay 旧网格 / 合集重训 / Adam / 整段 BPTT
- 把 `Doc/A0-a` 的整片 SRAM 当成算法配额；为 MCU / PPU 写固件

---

## 10. 文档与代码入口

| 路径 | 角色 |
|------|------|
| `Doc/03-a` | 增量分类与坑 |
| `Doc/03-b` | 不换骨干；滑窗是协议 |
| `Doc/03-c` | 开环损失、门控 |
| `Doc/03-d` | 残差头、10 mV 规则 |
| `Doc/04-a` | 第 0 期 A–H 对照数字 |
| `Doc/04-b` | 增量应用手册 |
| `Doc/04-c` | 第 0 期结题汇报（可分享） |
| `Doc/05-a` | 车上增量约束：无 OTA、数据不外发、单 MCU 信封 |
| `Doc/05-b` | 车上增量方案头脑风暴（主线仍是 \(k\) 网格） |
| `Doc/05-c` | TC4D7 资源 / 负载；C 在 `Src/MCU_Eval` |
| `Doc/05-d` | 第 1 期精度；默认写网格，1c 丢掉 |
| `Doc/05-e` | 第 1 期结题汇报（可分享） |
| `Doc/A0-a` | 包级、TC4D7 |
| `Doc/A0-b` | ST / NXP AI MCU 与 TC4D7 对照 |
| `Src/AI/KF/increment.py` | 实验室 Replay / 缩放 / 微调（车上不走这里） |
| `Src/AI/EV_Local/window.py` | 1a 滑窗全局 \(k_0,k_1\) |
| `Src/AI/EV_Local/kgrid.py` | 1b 滑窗 5×4 \(k\) 网格 |
| `Src/AI/EV_Local/head.py` | 1c 3×8×2 残差头（对照，已丢掉） |
| `Src/AI/KF/hole.py` | 任务 B 填洞：拆网格 + 洞舰队 + 四档 |
| `Src/AI/KF/gate.py` | 增量门控 |
| `Src/Sim/nmc100ah_ecm*.py` | 生成器；2RC / SOH 只加在这里 |

第 0 期数字在 [`Doc/04-a`](Doc/04-a-合成增量对照实验.md)，用法在 [`Doc/04-b`](Doc/04-b-增量学习应用手册.md)，汇报在 [`Doc/04-c`](Doc/04-c-第0期实验汇报.md)。车上约束在 [`Doc/05-a`](Doc/05-a-车上增量约束评估.md)，第 1 期精度在 [`Doc/05-d`](Doc/05-d-车上增量精度评估.md)，汇报在 [`Doc/05-e`](Doc/05-e-第1期实验汇报.md)。本计划只保留分期与对错标准。
